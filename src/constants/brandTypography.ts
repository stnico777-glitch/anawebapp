/**
 * "power love sound mind" — matches HeroTitle subtitle (Poppins, weight, letter-spacing)
 * for light surfaces (auth cards). Hero on video uses cream + stroke; auth uses gray body color.
 */
export const HERO_TAGLINE_AUTH_CLASS =
  "mt-2 text-sm font-medium lowercase text-gray [font-family:var(--font-poppins),sans-serif] [font-synthesis:none] tracking-[0.32em] md:tracking-[0.38em]";
